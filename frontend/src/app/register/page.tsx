import RegisterForm from "@/components/form/RegisterForm";
import React from "react";

const Register = () => {
  return (
    <main>
      <RegisterForm />
    </main>
  );
};

export default Register;
